package hkmu.wadd.model;
/*
import java.io.Serializable;
import java.util.Objects;

public class VoteId implements Serializable {
    private String username;
    private Long pollId;

    public VoteId(String username, Long pollId) {
        this.username = username;
        this.pollId = pollId;
    }

    public String getUsername() {
        return username;
    }

    public void setUsername(String username) {
        this.username = username;
    }

    public long getPollId() {
        return pollId;
    }

    public void setPollId(long pollId) {
        this.pollId = pollId;
    }

    @Override
    public int hashCode() {
        return Objects.hash(username, pollId);
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj) return true;
        if (!(obj instanceof VoteId)) return false;
        VoteId voteId = (VoteId) obj;
        return username.equals(voteId.username) && pollId.equals(voteId.pollId);
    }
}
*/